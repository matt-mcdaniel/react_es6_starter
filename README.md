## Easy Setup

Clone repo with `git clone https://github.com/matt81m/react_es6_starter.git`.

Run `npm install` to install dependencies.

Start webpack development server with `npm run serve`.

Navigate to `http://localhost:8080/webpack-dev-server/` in your browser.

Start coding.